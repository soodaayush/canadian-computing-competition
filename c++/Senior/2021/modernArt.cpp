#include <iostream>
#include <vector>
#include <unordered_set>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int m, n, k;

    cin >> m;
    cin >> n;
    cin >> k;

    unordered_set<int> rows;
    unordered_set<int> cols;
}